

export interface MapRecord {
  [key: string | symbol]: any
}

/**
 * You are to flatten Map<string, MapRecord>
 * to Array<[string, MapRecord]>
 */
export function flatten(map: Map<string, MapRecord>): Array<[string, MapRecord]> {
  
  return [];
}
